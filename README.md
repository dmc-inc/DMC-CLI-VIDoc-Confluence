# DMC-CLI-VIDoc-Confluence
Adds a command line interface using g-CLI to the VIDoc Confluence package

This will be where the CLI implimentation for VIDoc Confluence goes once properly split up. 
Will include Git interaction as it is assumed there is some level of pipeline interaction with CLI. 
